Requirements:
    - python-pip

How to run:

1. Install python requirements:
    pip install -r requirements.txt

2. Run migration:
    python manage.py migrate

3. Create superuser for next manipulations:
    python manage.py createsuperuser

4. Run server:
    python manage.py runserver

You can find the swagger documentation on the root URL (for local server: http://127.0.0.1:8000/).